# 复制本文件为 keystore.properties（不要提交到 git）
#
# storeFile 建议用相对路径，避免换机器/盘符就坏：
#   storeFile=keystore/atri-release.jks
#
# 然后在 Windows 里生成 keystore（示例，确保 keytool 在 PATH 里）：
#   keytool -genkeypair -v ^
#     -keystore keystore\atri-release.jks -alias atri ^
#     -keyalg RSA -keysize 2048 -validity 10000
#
storeFile=keystore/atri-release.jks
storePassword=请填你的密码
keyAlias=atri
keyPassword=请填你的密码
